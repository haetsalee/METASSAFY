import styled from 'styled-components';

const BoardNavbar = () => {
  return <SectionStyle>넷바</SectionStyle>;
};

export default BoardNavbar;

const SectionStyle = styled.section`
  background-color: red;
  width: 20%;
`;
