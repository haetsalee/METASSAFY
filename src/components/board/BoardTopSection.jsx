import styled from 'styled-components';

const BoardTopSection = () => {
  return <SectionStyle>hi</SectionStyle>;
};

export default BoardTopSection;

const SectionStyle = styled.section`
  background-color: pink;
  height: 20rem;
`;
